<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Loan Number Input -->
        <EditText
            android:id="@+id/etLoanNumber"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Indtast lånenummer"
            android:inputType="number" />

        <!-- Search Button -->
        <Button
            android:id="@+id/btnSearchLoan"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Søg lån" />

        <!-- Loan List Header -->
        <TextView
            android:id="@+id/tvLoanListHeader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Vælg lån til aflevering"
            android:textStyle="bold"
            android:visibility="gone" />

        <!-- Loan List -->
        <LinearLayout
            android:id="@+id/loanListLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone" />

        <!-- Return Loan Button -->
        <Button
            android:id="@+id/btnReturnLoan"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Returner Lån"
            android:visibility="gone" />
    </LinearLayout>
</ScrollView>
